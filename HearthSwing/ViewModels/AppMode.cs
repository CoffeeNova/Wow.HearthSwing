namespace HearthSwing.ViewModels;

/// <summary>
/// Top-level UI mode: the classic per-account view or the cross-account character templates view.
/// </summary>
public enum AppMode
{
    Accounts,
    Templates,
}
